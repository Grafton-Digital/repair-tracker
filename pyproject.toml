[project]
name = "repair-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.115.12",
    "pathlib>=1.0.1",
    "python-dotenv>=1.1.0",
    "sqlmodel>=0.0.24",
]
